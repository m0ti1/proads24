# DESIGN.md — proads24

Записано по факту сборки (`index.html`, `css/style.css`, `js/*.js`), не по плану.

## World

Тёмная «сеть роста»: чёрный фон, синие разомкнутые кольца и узлы, зелёная стрела.
Мир выведен из фирменного лого и референса (изометрическая сеть узлов на чёрном).
Витрина живых кампаний в hero — главный носитель правдоподобия.

## Palette

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#07090c` | Фон страницы |
| `--bg-soft` | `#0c0f13` | Чередование секций (`.section-alt`) |
| `--panel` / `--panel-2` | `#10141a` / `#151a22` | Карточки (лёгкий вертикальный градиент) |
| `--line` / `--line-soft` | `#222a35` / `#1a212b` | Границы 1px, единственная система высот + мягкая тень `--shadow-card` |
| `--blue` / `--blue-2` | `#1e6fe8` / `#4e92ff` | Бренд, primary-кнопки, числовые бейджи |
| `--green` / `--green-2` | `#3ac85a` / `#7bf29b` | Акцент роста: галочки, «живые» пилюли, успешные состояния |
| `--text` / `--text-2` / `--muted` | `#f2f5f9` / `#b7c2d0` / `#93a0b0` | Текст трёх уровней |

Правило: зелёный — только «рост/успех/живое», синий — «действие/бренд». Не смешивать.

## Type

- Единственное семейство: **Onest** (Google Fonts), веса 400–800.
- H1 `clamp(2.1rem→3.9rem)` w800; H2 `clamp(1.55→2.5rem)` w700; H3 1–1.3rem w700.
- Body 16px/1.6; трекинг заголовков −0.02…−0.025em.
- Все цифры метрик — `font-variant-numeric: tabular-nums`.

## Named rules

1. **Пилюля-номер**: круглая рамка 1.5px синим, tabular-nums; в активном шаге процесса перекрашивается в зелёный.
2. **Карточка**: `--panel`, граница `--line`, радиус 16px (12px для мелких), тень `--shadow-card`, hover — `translateY(-3…-4px)` + светлее граница. Без border-left полос.
3. **Hero-фон**: SVG-сеть узлов (кольцо + точка) + два radial-градиента бренда; узлы медленно плавают (11–17s, `translate`).
4. **Появление**: `.rise` с экспоненциальным ease-out `cubic-bezier(.19,1,.22,1)`, лесенка задержек 0.05–0.35s. Анимируются только transform/opacity.
5. **Метрики в hero-витрине**: цифры крупно, подпись снизу мелким uppercase, `tabular-nums`.
6. **Формы**: тёмные поля на `--bg-soft`, фокус — синяя рамка + мягкое кольцо `rgba(78,146,255,.18)`; ошибки — `--red` + текст причины.
7. **Модалки**: нативный `<dialog>`, радиус 18px, sticky-шапка, backdrop с blur.
8. **Браузерные поверхности темизированы**: selection, placeholder, фокус-ринги, скроллбар.
9. **Адаптив**: 1080 / 960 (мобильное меню off-canvas) / 640.
10. **`[hidden] { display:none !important }`** — атрибут hidden всегда сильнее display-правил.
11. `prefers-reduced-motion` гасит анимации и плавный скролл.

## Not canonized

Секционные номера-«очки» (01–07) в заголовках оставлены как навигационный мотив
сайта-образца syntez24, не как общий принцип типографики: в новых поверхностях
номера не добавлять без нумерованной последовательности по смыслу.
